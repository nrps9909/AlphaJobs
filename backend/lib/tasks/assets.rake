namespace :assets do
  desc "Precompile assets"
  task :precompile do
    puts "No assets to precompile for API-only application"
  end
  
  desc "Clean assets"
  task :clean do
    puts "No assets to clean for API-only application"
  end
end
